# WeaponFinder V1.1 Release Notes
## Key Updates from V1.0
1. Model: Added parameter validation in setters (data integrity)
2. Store: Replaced List with Array (meet project requirement)
3. UI: Replaced Scanner with JOptionPane + input validation (better UX)
4. Search: Added partial ID match search + power sorting (bubble sort)

## How to Run
Run `ui.MenuUI.main()` in IntelliJ IDEA (operate via pop-up windows).

## Compatibility
Fully compatible with V1.0 core logic.